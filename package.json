{
  "name": "veresk-ritz",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "npm run generate && rsync -avh --delete --exclude=.DS_Store dist/ reg.ru:freshnet/veresk/"
  },
  "dependencies": {
    "@crazydos/vue-markdown": "^1.0.2",
    "@nuxt/image": "^2.0.0",
    "moment": "^2.30.1",
    "nuxt": "^4.4.8",
    "vue": "^3.5.35",
    "vue-router": "^5.1.0"
  }
}
